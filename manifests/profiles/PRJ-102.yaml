# manifests/profiles/PRJ-102.yaml
# KRAS G12C Resistance — Dr. Liwei Rong

project_id: PRJ-102
pi: Dr. Liwei Rong
domain: oncology

model_stack:
  - skill_id: LocalLLM-DNABERT-2
    owner: platform
  - skill_id: SK-001                 # DNABERT-2 oncology-somatic-v2 — shared
    owner: shared
    lab_share_pct: 60
  - skill_id: LocalLLM-RAG
    owner: platform

knowledge_sources:
  pubmed_query: "KRAS G12C inhibitor resistance mechanisms"
  pdb_targets: ["6XR8"]
  private_lake: false

fine_tuning:
  method: LoRA
  lora_config:
    r: 8
    alpha: 16
    dropout: 0.1
  mini_pairs_threshold: 200

hypothesis_engine:
  scan_targets:
    - "MAPK reactivation"
    - "secondary mutations"
    - "SOS1 combination"
  min_confidence: 0.60

benchmarks:
  ground_truth_pairs: "BRCA_eval"
  threshold: 200

grants:
  - grant_id: GR-002
    type: NSERC
    rag_enabled: false

ip_rules:
  base_models: platform
  lab_skills:  shared
  shared_threshold_hrs: 8
